def multiply(*args):
    product = 1
    for arg in args:
        product *= arg
    return product

    # or -      product = product * arg 
    # I am still getting used to the *= shortcut with ALL operators : )